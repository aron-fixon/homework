const { } = require('../models/user');
const { } = require('../helpers/httpStatus');
const { CryptPassword } = require('../utils/encrypter');
const { } = require('../utils/errorHandler');
class UsersService {
    static createNewUser() {
        encryptPassword
    }
}
